config_name: "wikipedia-en-2K-s42_RTX3090_e10"

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}_${config_name}

defaults:
  - wikipedia-en-2K-s42_RTX3090
  - _self_

train:
  num_epochs: 10
