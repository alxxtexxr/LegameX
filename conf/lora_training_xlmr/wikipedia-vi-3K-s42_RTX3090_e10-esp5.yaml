config_name: "wikipedia-vi-3K-s42_RTX3090_e10-esp5"

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}_${config_name}

defaults:
  - wikipedia-vi-3K-s42_RTX3090
  - _self_

train:
  num_epochs: 10
  early_stopping_patience: 5
